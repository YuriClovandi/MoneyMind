{
  "name": "moneymind",
  "version": "1.0.0",
  "description": "MoneyMind - Sistema de controle financeiro pessoal",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run frontend:dev\" \"npm run backend:dev\"",
    "frontend:dev": "cd frontend && npm start",
    "backend:dev": "cd backend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "backend:build": "cd backend && npm run build",
    "backend:start": "cd backend && npm start",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build:all": "npm run backend:build && npm run frontend:build",
    "build": "npm run build:all",
    "start:production": "cd backend && node dist/index.js",
    "start": "npm run start:production",
    "heroku-postbuild": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../backend && npm run build && cd ../frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "react",
    "typescript",
    "express",
    "supabase",
    "finance",
    "money-management"
  ],
  "author": "",
  "license": "MIT"
}
